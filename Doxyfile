/**
 * \brief Рассчитывает суммарную массу папки.
 *
 * \param repo Репозиторий, через который выполняется получение данных
 *             из базы данных.
 * \param folder_id Идентификатор папки, массу которой необходимо
 *                  определить.
 *
 * \return Суммарная масса содержимого папки в граммах.
 *
 * Функция получает детали и вложенные папки. Для каждой детали
 * учитываются масса одного экземпляра и количество. Затем функция
 * рекурсивно рассчитывает массу вложенных папок и добавляет её
 * к общей массе.
 */
folder_mass(...)
/**
 * \brief Создаёт новую деталь в текущей папке.
 *
 * \param c Callback-запрос Telegram, содержащий идентификатор папки.
 * \param state Текущее FSM-состояние диалога с пользователем.
 *
 * Функция вызывается после нажатия кнопки добавления детали.
 * Идентификатор текущей папки сохраняется в FSM, после чего бот
 * переводит пользователя в состояние ввода названия детали.
 */
component_new(...)
/**
 * \brief Создаёт новый проект пользователя.
 *
 * \param telegram_id Telegram ID владельца проекта.
 * \param name Название создаваемого проекта.
 * \param description Необязательное описание проекта.
 * \param weight_limit_g Необязательное ограничение массы в граммах.
 *
 * \return Идентификатор созданного проекта.
 *
 * Метод сначала получает внутренний идентификатор пользователя,
 * после чего создаёт запись проекта в SQLite и автоматически
 * создаёт его корневую папку.
 */
create_project(...)